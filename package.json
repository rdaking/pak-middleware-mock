{
  "name": "prism-mock",
  "version": "1.0.0",
  "scripts": {
    "start": "npx prism mock openapi.yaml --cors --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "@stoplight/prism-cli": "4.10.5"
  }
}
